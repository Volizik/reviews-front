import { DefaultTheme } from 'styled-components'

const CustomTheme: DefaultTheme = {
    borderRadius: '5px',

    colors: {
        main: 'cyan',
        secondary: 'magenta',
    },
};

export { CustomTheme }